providers = ["node"]

[variables]
NODE_VERSION = "22.12.0"
NIXPACKS_NODE_VERSION = "22.12.0"

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
